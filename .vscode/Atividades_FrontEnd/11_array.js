const nomes = ['João Henrique', 'Sara', 'Fábio', 'Alexandre', 'Filipe'];

nomes.forEach(function(nomes) {
    console.log(nomes)
});


for (var contador = nomes.length-1; contador>=0; contador--) {
    console.log(nomes[contador]);
}